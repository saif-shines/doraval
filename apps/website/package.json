{
  "name": "@doraval/website",
  "private": true,
  "type": "module",
  "version": "0.6.5",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "blume dev",
    "build": "blume build && bun run scripts/check-site.ts",
    "preview": "blume preview",
    "check": "bun run scripts/check-site.ts"
  },
  "dependencies": {
    "blume": "^1.0.3"
  }
}
