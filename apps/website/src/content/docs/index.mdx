---
title: Scale your AI context for coding agents
description: Scan, review, fix, and remember agent context so skills and plugins work on the first try — across Claude, Cursor, Codex, Copilot, and Grok.
template: splash
hero:
  title: Scale your AI context for coding agents
  tagline: Context you cannot trust wastes a day. doraval makes the first attempt succeed — across Claude, Cursor, Codex, Copilot, and Grok.
  actions:
    - text: Get Started
      link: /get-started/
      icon: right-arrow
    - text: GitHub
      link: https://github.com/saif-shines/doraval
      variant: minimal
      icon: external
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

## Try it

Zero config. No API key. Scans agent surfaces, skill health, and the next command to run.

```bash
npx @hacksmith/doraval
# after install: dora
```

```text
$ dora

  doraval v0.6.1
  Read-only scan of agent context — no writes, no LLM.

  Agent surfaces
    ✓ claude    CLAUDE.md  .claude/skills
    ⚠ cursor    not configured

  Health
    ✓ .claude/skills/review    valid
    ✗ .claude/skills/deploy    Missing "description"

  Next
    1. dora fix .claude/skills/deploy
    2. dora review --all
```

## Install

<Tabs>
  <TabItem label="Homebrew">
    ```bash
    brew tap saif-shines/tap
    brew trust saif-shines/tap
    brew install doraval
    ```
  </TabItem>
  <TabItem label="npm">
    ```bash
    npm install -g @hacksmith/doraval
    # or: npx @hacksmith/doraval
    ```
  </TabItem>
  <TabItem label="Bun">
    ```bash
    bun add -g @hacksmith/doraval
    # or: bunx @hacksmith/doraval
    ```
  </TabItem>
</Tabs>

Alias after install: `dora` (same CLI as `doraval`).

## Then

```bash
dora                              # scan
dora new skill --for claude --name review-pr --description "Reviews PRs" --yes
dora review .                     # structure + heuristics (+ LLM when available)
dora fix .                        # diffs first; asks before writing
dora memory add "Run tests before shipping skill changes" --weight 8
```

Full path in the [quickstart](/get-started/quickstart/). Deeper docs: [memory](/concepts/memory/), [review tiers](/concepts/three-tier-verification/).
