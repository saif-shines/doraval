# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# built CLI bundle (generated by `bun run build`; rebuilt by CI + prepublishOnly,
# never committed to avoid perpetual dirty diffs — see code-style review plan)
/bin/doraval.js
/bin/ui/

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# astro generated
.astro
package-lock.json

# local docs (not published)
/docs/
plans/

# worktrees for isolated feature development (using-git-worktrees skill)
.worktrees/
worktrees/
.superpowers/

# test temporary directories (created by validators.test.ts etc. for isolation)
test/tmp-*
# legacy root-level test artifacts (now moved under test/)
tmp-*
my-test-plugin/

.agents/
.claude/