---
name: crlf-lines
description: Use when testing Windows CRLF line endings.
---

# CRLF Skill

1. Run tests on Windows
2. Verify frontmatter parsing

Ensure parsing works. MUST handle CRLF. MUST NOT assume LF only.

```bash
echo crlf
```
